# Módulo de acoplamiento

El contrato HDF5 vive en `schemas/`; la implementación Python reutilizable vive
en `src/cavitation_coupling/`. Los fixtures cruzados de solver se añadirán aquí
solo cuando tengan licencia y tamaño aptos para Git.
